import 'package:flutter/material.dart';

class wheels extends StatelessWidget {
  const wheels({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Learn Two Wheelers Driving"),
      ),
      backgroundColor: Colors.amberAccent,
      body: Container(),
    );
  }
}
