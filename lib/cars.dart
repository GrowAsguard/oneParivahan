import 'package:flutter/material.dart';

class cars extends StatelessWidget {
  const cars({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Learn Cars Driving"),
      ),
      backgroundColor: Colors.amberAccent,
      body: Container(),
    );
  }
}
